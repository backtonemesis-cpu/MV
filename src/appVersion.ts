/**
 * Human-readable MV application release number.
 *
 * This is intentionally separate from HouseholdData.version, which tracks
 * local financial-data mutations. Increment this value when a new MV app
 * release is deployed.
 */
export const APP_VERSION = '213';
